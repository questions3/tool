# Convvy — визуальная система

Режим: **Operate**. Оператор в задаче, инструмент обязан исчезать. Бренд
живёт в точности деталей, а не в эффектах.

## Градиент — подпись, а не заливка

Фиолетово-синий перелив логотипа появляется ровно в четырёх местах:

1. знак в шапке и на экране входа;
2. первичное действие (одна кнопка на экран);
3. индикатор текущего шага и текущего выбора;
4. волосяная линия под шапкой.

Больше нигде. Градиент, размазанный по фонам и карточкам, перестаёт быть
подписью и превращается в шум — а на экране, где оператор ищет строку под
голос клиента, шум стоит секунд.

```
--brand-violet: #8B2BF2
--brand-blue:   #1E5BFF
--brand:        linear-gradient(135deg, violet, blue)
```

Для интерактива нужен сплошной цвет, а не градиент: точка перелива
`#5741F8` (6.0:1 на белом — проходит для текста).

## Нейтраль подкрашена, а не сера

Серый рядом с фиолетово-синим выглядит грязным. Вся нейтраль уведена в
холодный сине-фиолетовый: чернила `#0E1330` — тот самый навий из
начертания «Convvy».

| Токен | Значение | Контраст на белом |
|---|---|---|
| `ink` | `#0E1330` | 17.9:1 |
| `ink-2` | `#3C4166` | 9.8:1 |
| `ink-3` | `#6B7096` | 4.9:1 |
| `line` | `#E4E5F0` | — |
| `canvas` | `#F7F7FC` | — |
| `panel` | `#F2F2FA` | второй слой: шапка, панели |

## Типографика

Одна гарнитура — Inter. Продуктовому интерфейсу не нужна пара
display/body: элементов много, и контраст начертаний создаёт шум.

Шкала фиксированная в rem, не fluid: оператор смотрит с одного расстояния,
и заголовок, который дышит вместе с шириной окна, здесь только мешает.
Шаг 1.2. Цифры в таблицах — табличные.

## Движение

150–250 мс, `cubic-bezier(0.22, 1, 0.36, 1)`. Движение сообщает состояние:
наведение, фокус, появление ответа, смена шага. Ничего декоративного,
никаких оркестрованных загрузок страницы — оператор входит в задачу, а не
смотрит представление.

Загрузка — скелетоны в форме будущего контента, не спиннер посреди экрана.

## Иконки

Рисованные SVG, штрих 1.5 на сетке 20, `currentColor`. Эмодзи и юникодные
глифы (`✕`, `⌕`, `★`, `→`) из интерфейса убраны: они меняют начертание от
платформы к платформе и не бывают одной толщины.

## Поверхности браузера

Выделение, каретка, полосы прокрутки, кольцо фокуса и подчёркивание взяты
из палитры. Это то, что отличает собранный интерфейс от нарисованного.

## Запрещено в этом мире

- Градиентный текст. Акцент — весом и размером.
- Кикер над заголовком. Заголовок несёт себя сам.
- Цветная полоса слева у карточек и плашек толще 1px.
- Тень без смещения (цветной ореол вместо глубины).
- Модалка там, где хватает раскрытия по месту.
