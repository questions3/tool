# Резервный SPA-fallback (дублирует правило из netlify.toml).
# Нужен, когда появится клиентский роутер: прямой заход на /path
# или перезагрузка должны отдавать index.html, а не 404.
/*  /index.html  200
